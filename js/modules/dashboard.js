export async function initDashboard(session) {
  const container = document.getElementById('dashboard-root');
  if (container) container.innerHTML = '<div class="empty-state"><p>Dashboard ejecutivo — Fase 4</p></div>';
}
