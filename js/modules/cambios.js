export async function initCambios(session) {
  const container = document.getElementById('cambios-root');
  if (container) container.innerHTML = '<div class="empty-state"><p>Módulo Cambios de Medidores — Fase 2</p></div>';
}
