export async function initOtc(session) {
  const container = document.getElementById('otc-root');
  if (container) container.innerHTML = '<div class="empty-state"><p>Módulo OTC — Fase 3</p></div>';
}
